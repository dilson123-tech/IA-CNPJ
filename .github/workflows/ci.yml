name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Install deps
        run: |
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Ruff
        run: ruff check .

      - name: Py compile
        run: python -m compileall -q .
      - name: Install shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Shell scripts (bash -n + shellcheck)
        shell: bash
        run: |
          set -euo pipefail
          find scripts -type f -name '*.sh' -print0 | xargs -0 -r -n1 bash -n
          find scripts -type f -name '*.sh' -print0 | xargs -0 -r shellcheck -x -S error

  smoke:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Smoke (make dev)
        env:
          DATABASE_URL: sqlite:////tmp/ia_cnpj_ci.db
        shell: bash
        run: |
          set -euo pipefail
          set -o xtrace
          export SHELLOPTS

          # sanity: bash filho herda xtrace via SHELLOPTS
          bash -lc '[[ $- == *x* ]] && echo "xtrace inherited ✅"'

          make dev |& tee /tmp/ci_smoke.log

          # segurança: NÃO pode vazar header Authorization nem JWT (prefixo eyJhbGci)
          if grep -nE 'Authorization: Bearer|eyJhbGci' /tmp/ci_smoke.log; then
            echo "❌ SECURITY: token vazou no log sob xtrace"
            exit 1
          fi

          echo "✅ OK: não vazou token sob xtrace"
